'use strict'

describe('ModuleName', function() {
  beforeAll(() => {})
  afterAll(() => {})
  describe('default properties', () => {
    test('should have ...', done => {
      expect(true).toBeTrue
      done()
    })
  })
})